object ThirdMaximumNumber414 {
  def thirdMax(nums: Array[Int]): Int = {
    var threeNums = Array(-Int.MaxValue, -Int.MaxValue, -Int.MaxValue)
    nums.foreach(num => {
//      if num > threeNums(1) || num >
    })

    1
  }
  def main(args: Array[String]): Unit = {
    println("Hello, world!")
  }
}
